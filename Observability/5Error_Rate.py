""" 
=> Error Rate 
    
    -> how many request are failing compared to total request
         
         error_rate = (failed requests) / (total requests)
    
    -> Example 
        
        Total Request = 1000
        Failed = 50 
        
        Error rate = 50 / 1000 = 0.05 = 5%    
        
      
      
      
      rate(http_requests_total[5m])
      
      “Average number of requests per second, calculated over the last 5 minutes”   

"""
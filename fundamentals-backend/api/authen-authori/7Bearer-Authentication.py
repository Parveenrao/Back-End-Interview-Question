""" 


=> Bearer Authentication

    -> Instead of sending the username and password with every request , the client sends 
       a token


    -> word bearer means

        Whoever bears (posseses) this token is treated as authenticated

    -> server does not know  who is holding the token. if the token is valid access is granted        


"""
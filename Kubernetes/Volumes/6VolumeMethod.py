""" 

=> VolumeMethod 

   Acessmethod = who can use the storage 
   Volume mode = who can access the storage

=> A volume mode specifies how kubernetes expose the storage to a POd

=> There are only two modes

    1. Filesystem(Default)

    2. Block 



=> Filesystem 

    -> Is a way of organizing and manging data on a storage device (HDD, SDD , USB)

    -> Where each file is stored 

    -> What the file name is 

    -> which folder exist 

    -> Who own the file


=> Block 

  -> A block has no filesystem at all

  -> A block device is a storage device that store data in fixed size chunk called blocks

  -> block is small fixed size of storage


"""